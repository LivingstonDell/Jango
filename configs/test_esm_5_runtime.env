# Compatibility shim. Canonical ESMFold2 test runtime config: configs/test/esmfold2.env
_jango_config_dir="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/test/esmfold2.env
source "${_jango_config_dir}/test/esmfold2.env"
unset _jango_config_dir
