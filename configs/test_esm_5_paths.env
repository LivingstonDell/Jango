# Compatibility shim. Canonical test path config: configs/test/paths.env
_jango_config_dir="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/test/paths.env
source "${_jango_config_dir}/test/paths.env"
unset _jango_config_dir
